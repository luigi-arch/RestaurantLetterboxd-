/**
 * Product naming — a PLACEHOLDER, deliberately kept in one file.
 *
 * "Mejda" is Maltese for table. It is short, local, and not literal, which is
 * roughly what the name wants to be — but it was chosen as a working title, not
 * decided. Change these three values and the whole app follows; nothing else
 * hardcodes the name.
 *
 * Worth settling before lists start getting shared, since those carry the brand
 * outward. Check the domain first.
 */
export const BRAND = {
  name: "Mejda",
  tagline: "A diary of eating out in Malta",
  description:
    "Log every meal you eat out in Malta, follow the people whose taste you trust, and fill in the map.",
} as const;
